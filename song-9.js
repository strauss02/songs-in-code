class Road {
  personWalking

  construcor(isLonely, isHome) {
    this.isLonely = isLonely
    this.isHome = isHome
  }
  walk(person) {
    personWalking = person
  }
}

const lonelyRoad = new Road(true, true)

const I = alone

lonelyRoad.walk(I)
